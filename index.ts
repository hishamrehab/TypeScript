type Person= {
    name :string;
};

const person :Person = {
    name: "John"
}

console.log(person.name);

// Annotation

// ket myVar = value;

let myName:string = "Huxn WebDev"

myName = "Another Name";
console.log(myName);

// Error 
myName = 12;